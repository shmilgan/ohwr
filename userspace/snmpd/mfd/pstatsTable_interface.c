/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 18978 $ of $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "pstatsTable.h"


#include <net-snmp/agent/table_container.h>
#include <net-snmp/library/container.h>

#include "pstatsTable_interface.h"

#include <ctype.h>

/**********************************************************************
 **********************************************************************
 ***
 *** Table pstatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * WR-SWITCH-MIB::pstatsTable is subid 2 of wrsPstats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.96.100.2.2, length: 10
 */
typedef struct pstatsTable_interface_ctx_s {

    netsnmp_container *container;
    netsnmp_cache  *cache;

    pstatsTable_registration *user_ctx;

    netsnmp_table_registration_info tbl_info;

    netsnmp_baby_steps_access_methods access_multiplexer;

} pstatsTable_interface_ctx;

static pstatsTable_interface_ctx pstatsTable_if_ctx;

static void     _pstatsTable_container_init(pstatsTable_interface_ctx *
                                            if_ctx);
static void     _pstatsTable_container_shutdown(pstatsTable_interface_ctx *
                                                if_ctx);


netsnmp_container *
pstatsTable_container_get(void)
{
    return pstatsTable_if_ctx.container;
}

pstatsTable_registration *
pstatsTable_registration_get(void)
{
    return pstatsTable_if_ctx.user_ctx;
}

pstatsTable_registration *
pstatsTable_registration_set(pstatsTable_registration * newreg)
{
    pstatsTable_registration *old = pstatsTable_if_ctx.user_ctx;
    pstatsTable_if_ctx.user_ctx = newreg;
    return old;
}

int
pstatsTable_container_size(void)
{
    return CONTAINER_SIZE(pstatsTable_if_ctx.container);
}

/*
 * mfd multiplexer modes
 */
static Netsnmp_Node_Handler _mfd_pstatsTable_pre_request;
static Netsnmp_Node_Handler _mfd_pstatsTable_post_request;
static Netsnmp_Node_Handler _mfd_pstatsTable_object_lookup;
static Netsnmp_Node_Handler _mfd_pstatsTable_get_values;
/**
 * @internal
 * Initialize the table pstatsTable 
 *    (Define its contents and how it's structured)
 */
void
_pstatsTable_initialize_interface(pstatsTable_registration * reg_ptr,
                                  u_long flags)
{
    netsnmp_baby_steps_access_methods *access_multiplexer =
        &pstatsTable_if_ctx.access_multiplexer;
    netsnmp_table_registration_info *tbl_info =
        &pstatsTable_if_ctx.tbl_info;
    netsnmp_handler_registration *reginfo;
    netsnmp_mib_handler *handler;
    int             mfd_modes = 0;

    DEBUGMSGTL(("internal:pstatsTable:_pstatsTable_initialize_interface",
                "called\n"));


    /*************************************************
     *
     * save interface context for pstatsTable
     */
    /*
     * Setting up the table's definition
     */
    netsnmp_table_helper_add_indexes(tbl_info, ASN_INTEGER,
                                               /** index: pstatsIndex */
                                     0);

    /*
     * Define the minimum and maximum accessible columns.  This
     * optimizes retrieval. 
     */
    tbl_info->min_column = PSTATSTABLE_MIN_COL;
    tbl_info->max_column = PSTATSTABLE_MAX_COL;

    /*
     * save users context
     */
    pstatsTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    pstatsTable_init_data(reg_ptr);

    /*
     * set up the container
     */
    _pstatsTable_container_init(&pstatsTable_if_ctx);
    if (NULL == pstatsTable_if_ctx.container) {
        snmp_log(LOG_ERR,
                 "could not initialize container for pstatsTable\n");
        return;
    }

    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_pstatsTable_object_lookup;
    access_multiplexer->get_values = _mfd_pstatsTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_pstatsTable_pre_request;
    access_multiplexer->post_request = _mfd_pstatsTable_post_request;


    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUGMSGTL(("pstatsTable:init_pstatsTable",
                "Registering pstatsTable as a mibs-for-dummies table.\n"));
    handler =
        netsnmp_baby_steps_access_multiplexer_get(access_multiplexer);
    reginfo =
        netsnmp_handler_registration_create("pstatsTable", handler,
                                            pstatsTable_oid,
                                            pstatsTable_oid_size,
                                            HANDLER_CAN_BABY_STEP |
                                            HANDLER_CAN_RONLY);
    if (NULL == reginfo) {
        snmp_log(LOG_ERR, "error registering table pstatsTable\n");
        return;
    }
    reginfo->my_reg_void = &pstatsTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if (access_multiplexer->object_lookup)
        mfd_modes |= BABY_STEP_OBJECT_LOOKUP;
    if (access_multiplexer->set_values)
        mfd_modes |= BABY_STEP_SET_VALUES;
    if (access_multiplexer->irreversible_commit)
        mfd_modes |= BABY_STEP_IRREVERSIBLE_COMMIT;
    if (access_multiplexer->object_syntax_checks)
        mfd_modes |= BABY_STEP_CHECK_OBJECT;

    if (access_multiplexer->pre_request)
        mfd_modes |= BABY_STEP_PRE_REQUEST;
    if (access_multiplexer->post_request)
        mfd_modes |= BABY_STEP_POST_REQUEST;

    if (access_multiplexer->undo_setup)
        mfd_modes |= BABY_STEP_UNDO_SETUP;
    if (access_multiplexer->undo_cleanup)
        mfd_modes |= BABY_STEP_UNDO_CLEANUP;
    if (access_multiplexer->undo_sets)
        mfd_modes |= BABY_STEP_UNDO_SETS;

    if (access_multiplexer->row_creation)
        mfd_modes |= BABY_STEP_ROW_CREATE;
    if (access_multiplexer->consistency_checks)
        mfd_modes |= BABY_STEP_CHECK_CONSISTENCY;
    if (access_multiplexer->commit)
        mfd_modes |= BABY_STEP_COMMIT;
    if (access_multiplexer->undo_commit)
        mfd_modes |= BABY_STEP_UNDO_COMMIT;

    handler = netsnmp_baby_steps_handler_get(mfd_modes);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = netsnmp_get_row_merge_handler(reginfo->rootoid_len + 2);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject container_table helper
     */
    handler =
        netsnmp_container_table_handler_get(tbl_info,
                                            pstatsTable_if_ctx.container,
                                            TABLE_CONTAINER_KEY_NETSNMP_INDEX);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject cache helper
     */
    if (NULL != pstatsTable_if_ctx.cache) {
        handler = netsnmp_cache_handler_get(pstatsTable_if_ctx.cache);
        netsnmp_inject_handler(reginfo, handler);
    }

    /*
     * register table
     */
    netsnmp_register_table(reginfo, tbl_info);

}                               /* _pstatsTable_initialize_interface */

/**
 * @internal
 * Shutdown the table pstatsTable
 */
void
_pstatsTable_shutdown_interface(pstatsTable_registration * reg_ptr)
{
    /*
     * shutdown the container
     */
    _pstatsTable_container_shutdown(&pstatsTable_if_ctx);
}

void
pstatsTable_valid_columns_set(netsnmp_column_info *vc)
{
    pstatsTable_if_ctx.tbl_info.valid_columns = vc;
}                               /* pstatsTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int
pstatsTable_index_to_oid(netsnmp_index * oid_idx,
                         pstatsTable_mib_index * mib_idx)
{
    int             err = SNMP_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * pstatsIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    netsnmp_variable_list var_pstatsIndex;

    /*
     * set up varbinds
     */
    memset(&var_pstatsIndex, 0x00, sizeof(var_pstatsIndex));
    var_pstatsIndex.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_pstatsIndex.next_variable = NULL;


    DEBUGMSGTL(("verbose:pstatsTable:pstatsTable_index_to_oid",
                "called\n"));

    /*
     * pstatsIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h 
     */
    snmp_set_var_value(&var_pstatsIndex, &mib_idx->pstatsIndex,
                       sizeof(mib_idx->pstatsIndex));


    err = build_oid_noalloc(oid_idx->oids, oid_idx->len, &oid_idx->len,
                            NULL, 0, &var_pstatsIndex);
    if (err)
        snmp_log(LOG_ERR, "error %d converting index to oid\n", err);

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers(&var_pstatsIndex);

    return err;
}                               /* pstatsTable_index_to_oid */

/**
 * extract pstatsTable indexes from a netsnmp_index
 *
 * @retval SNMP_ERR_NOERROR  : no error
 * @retval SNMP_ERR_GENERR   : error
 */
int
pstatsTable_index_from_oid(netsnmp_index * oid_idx,
                           pstatsTable_mib_index * mib_idx)
{
    int             err = SNMP_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * pstatsIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    netsnmp_variable_list var_pstatsIndex;

    /*
     * set up varbinds
     */
    memset(&var_pstatsIndex, 0x00, sizeof(var_pstatsIndex));
    var_pstatsIndex.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_pstatsIndex.next_variable = NULL;


    DEBUGMSGTL(("verbose:pstatsTable:pstatsTable_index_from_oid",
                "called\n"));

    /*
     * parse the oid into the individual index components
     */
    err = parse_oid_indexes(oid_idx->oids, oid_idx->len, &var_pstatsIndex);
    if (err == SNMP_ERR_NOERROR) {
        /*
         * copy out values
         */
        mib_idx->pstatsIndex = *((long *) var_pstatsIndex.val.string);


    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers(&var_pstatsIndex);

    return err;
}                               /* pstatsTable_index_from_oid */


/*
 *********************************************************************
 * @internal
 * allocate resources for a pstatsTable_rowreq_ctx
 */
pstatsTable_rowreq_ctx *
pstatsTable_allocate_rowreq_ctx(void *user_init_ctx)
{
    pstatsTable_rowreq_ctx *rowreq_ctx =
        SNMP_MALLOC_TYPEDEF(pstatsTable_rowreq_ctx);

    DEBUGMSGTL(("internal:pstatsTable:pstatsTable_allocate_rowreq_ctx",
                "called\n"));

    if (NULL == rowreq_ctx) {
        snmp_log(LOG_ERR, "Couldn't allocate memory for a "
                 "pstatsTable_rowreq_ctx.\n");
        return NULL;
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->pstatsTable_data_list = NULL;

    /*
     * if we allocated data, call init routine
     */
    if (!(rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER)) {
        if (SNMPERR_SUCCESS !=
            pstatsTable_rowreq_ctx_init(rowreq_ctx, user_init_ctx)) {
            pstatsTable_release_rowreq_ctx(rowreq_ctx);
            rowreq_ctx = NULL;
        }
    }

    return rowreq_ctx;
}                               /* pstatsTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a pstatsTable_rowreq_ctx
 */
void
pstatsTable_release_rowreq_ctx(pstatsTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("internal:pstatsTable:pstatsTable_release_rowreq_ctx",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    pstatsTable_rowreq_ctx_cleanup(rowreq_ctx);

    /*
     * free index oid pointer
     */
    if (rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp)
        free(rowreq_ctx->oid_idx.oids);

    SNMP_FREE(rowreq_ctx);
}                               /* pstatsTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_pstatsTable_pre_request(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    int             rc;

    DEBUGMSGTL(("internal:pstatsTable:_mfd_pstatsTable_pre_request",
                "called\n"));

    if (1 != netsnmp_row_merge_status_first(reginfo, agtreq_info)) {
        DEBUGMSGTL(("internal:pstatsTable",
                    "skipping additional pre_request\n"));
        return SNMP_ERR_NOERROR;
    }

    rc = pstatsTable_pre_request(pstatsTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("pstatsTable", "error %d from "
                    "pstatsTable_pre_request\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
}                               /* _mfd_pstatsTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_pstatsTable_post_request(netsnmp_mib_handler *handler,
                              netsnmp_handler_registration *reginfo,
                              netsnmp_agent_request_info *agtreq_info,
                              netsnmp_request_info *requests)
{
    pstatsTable_rowreq_ctx *rowreq_ctx = (pstatsTable_rowreq_ctx *)
        netsnmp_container_table_row_extract(requests);
    int             rc, packet_rc;

    DEBUGMSGTL(("internal:pstatsTable:_mfd_pstatsTable_post_request",
                "called\n"));

    /*
     * release row context, if deleted
     */
    if (rowreq_ctx && (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED))
        pstatsTable_release_rowreq_ctx(rowreq_ctx);

    /*
     * wait for last call before calling user
     */
    if (1 != netsnmp_row_merge_status_last(reginfo, agtreq_info)) {
        DEBUGMSGTL(("internal:pstatsTable",
                    "waiting for last post_request\n"));
        return SNMP_ERR_NOERROR;
    }

    packet_rc = netsnmp_check_all_requests_error(agtreq_info->asp, 0);
    rc = pstatsTable_post_request(pstatsTable_if_ctx.user_ctx, packet_rc);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("pstatsTable", "error %d from "
                    "pstatsTable_post_request\n", rc));
    }

    return SNMP_ERR_NOERROR;
}                               /* _mfd_pstatsTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_pstatsTable_object_lookup(netsnmp_mib_handler *handler,
                               netsnmp_handler_registration *reginfo,
                               netsnmp_agent_request_info *agtreq_info,
                               netsnmp_request_info *requests)
{
    int             rc = SNMP_ERR_NOERROR;
    pstatsTable_rowreq_ctx *rowreq_ctx = (pstatsTable_rowreq_ctx *)
        netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:pstatsTable:_mfd_pstatsTable_object_lookup",
                "called\n"));

    /*
     * get our context from mfd
     * pstatsTable_interface_ctx *if_ctx =
     *             (pstatsTable_interface_ctx *)reginfo->my_reg_void;
     */

    if (NULL == rowreq_ctx) {
        rc = SNMP_ERR_NOCREATION;
    }

    if (MFD_SUCCESS != rc)
        netsnmp_request_set_error_all(requests, rc);
    else
        pstatsTable_row_prep(rowreq_ctx);

    return SNMP_VALIDATE_ERR(rc);
}                               /* _mfd_pstatsTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_pstatsTable_get_column(pstatsTable_rowreq_ctx * rowreq_ctx,
                        netsnmp_variable_list * var, int column)
{
    int             rc = SNMPERR_SUCCESS;

    DEBUGMSGTL(("internal:pstatsTable:_mfd_pstatsTable_get_column",
                "called for %d\n", column));


    netsnmp_assert(NULL != rowreq_ctx);

    switch (column) {

        /*
         * (INDEX) pstatsIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSINDEX:
        var->type = ASN_INTEGER;
        var->val_len = sizeof(long);
        (*var->val.integer) = rowreq_ctx->tbl_idx.pstatsIndex;
        break;

        /*
         * pstatsDescr(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H 
         */
    case COLUMN_PSTATSDESCR:
        var->type = ASN_OCTET_STR;
        rc = pstatsDescr_get(rowreq_ctx, (char **) &var->val.string,
                             &var->val_len);
        break;

        /*
         * pstatsWR0(3)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR0:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR0_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR1(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR1:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR1_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR2(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR2:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR2_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR3(6)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR3:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR3_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR4(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR4:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR4_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR5(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR5:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR5_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR6(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR6:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR6_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR7(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR7:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR7_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR8(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR8:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR8_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR9(12)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR9:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR9_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR10(13)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR10:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR10_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR11(14)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR11:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR11_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR12(15)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR12:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR12_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR13(16)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR13:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR13_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR14(17)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR14:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR14_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR15(18)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR15:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR15_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR16(19)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR16:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR16_get(rowreq_ctx, (u_long *) var->val.string);
        break;

        /*
         * pstatsWR17(20)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_PSTATSWR17:
        var->val_len = sizeof(u_long);
        var->type = ASN_COUNTER;
        rc = pstatsWR17_get(rowreq_ctx, (u_long *) var->val.string);
        break;

    default:
        if (PSTATSTABLE_MIN_COL <= column && column <= PSTATSTABLE_MAX_COL) {
            DEBUGMSGTL(("internal:pstatsTable:_mfd_pstatsTable_get_column",
                        "assume column %d is reserved\n", column));
            rc = MFD_SKIP;
        } else {
            snmp_log(LOG_ERR,
                     "unknown column %d in _pstatsTable_get_column\n",
                     column);
        }
        break;
    }

    return rc;
}                               /* _pstatsTable_get_column */

int
_mfd_pstatsTable_get_values(netsnmp_mib_handler *handler,
                            netsnmp_handler_registration *reginfo,
                            netsnmp_agent_request_info *agtreq_info,
                            netsnmp_request_info *requests)
{
    pstatsTable_rowreq_ctx *rowreq_ctx = (pstatsTable_rowreq_ctx *)
        netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info *tri;
    u_char         *old_string;
    void            (*dataFreeHook) (void *);
    int             rc;

    DEBUGMSGTL(("internal:pstatsTable:_mfd_pstatsTable_get_values",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    for (; requests; requests = requests->next) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if (NULL == requests->requestvb->val.string) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->val_len =
                sizeof(requests->requestvb->buf);
        } else if (requests->requestvb->buf ==
                   requests->requestvb->val.string) {
            if (requests->requestvb->val_len !=
                sizeof(requests->requestvb->buf))
                requests->requestvb->val_len =
                    sizeof(requests->requestvb->buf);
        }

        /*
         * get column data
         */
        tri = netsnmp_extract_table_info(requests);
        if (NULL == tri)
            continue;

        rc = _pstatsTable_get_column(rowreq_ctx, requests->requestvb,
                                     tri->colnum);
        if (rc) {
            if (MFD_SKIP == rc) {
                requests->requestvb->type = SNMP_NOSUCHINSTANCE;
                rc = SNMP_ERR_NOERROR;
            }
        } else if (NULL == requests->requestvb->val.string) {
            snmp_log(LOG_ERR, "NULL varbind data pointer!\n");
            rc = SNMP_ERR_GENERR;
        }
        if (rc)
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if (old_string && (old_string != requests->requestvb->buf) &&
            (requests->requestvb->val.string != old_string)) {
            if (dataFreeHook)
                (*dataFreeHook) (old_string);
            else
                free(old_string);
        }
    }                           /* for results */

    return SNMP_ERR_NOERROR;
}                               /* _mfd_pstatsTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*
 * SET PROCESSING NOT APPLICABLE (per MIB or user setting)
 */
/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
static void     _container_free(netsnmp_container * container);

/**
 * @internal
 */
static int
_cache_load(netsnmp_cache * cache, void *vmagic)
{
    DEBUGMSGTL(("internal:pstatsTable:_cache_load", "called\n"));

    if ((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache for pstatsTable_cache_load\n");
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    netsnmp_assert((0 == cache->valid) || (1 == cache->expired));

    /*
     * call user code
     */
    return pstatsTable_container_load((netsnmp_container *) cache->magic);
}                               /* _cache_load */

/**
 * @internal
 */
static void
_cache_free(netsnmp_cache * cache, void *magic)
{
    netsnmp_container *container;

    DEBUGMSGTL(("internal:pstatsTable:_cache_free", "called\n"));

    if ((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache in pstatsTable_cache_free\n");
        return;
    }

    container = (netsnmp_container *) cache->magic;

    _container_free(container);
}                               /* _cache_free */

/**
 * @internal
 */
static void
_container_item_free(pstatsTable_rowreq_ctx * rowreq_ctx, void *context)
{
    DEBUGMSGTL(("internal:pstatsTable:_container_item_free", "called\n"));

    if (NULL == rowreq_ctx)
        return;

    pstatsTable_release_rowreq_ctx(rowreq_ctx);
}                               /* _container_item_free */

/**
 * @internal
 */
static void
_container_free(netsnmp_container * container)
{
    DEBUGMSGTL(("internal:pstatsTable:_container_free", "called\n"));

    if (NULL == container) {
        snmp_log(LOG_ERR,
                 "invalid container in pstatsTable_container_free\n");
        return;
    }

    /*
     * call user code
     */
    pstatsTable_container_free(container);

    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR(container,
                    (netsnmp_container_obj_func *) _container_item_free,
                    NULL);
}                               /* _container_free */

/**
 * @internal
 * initialize the container with functions or wrappers
 */
void
_pstatsTable_container_init(pstatsTable_interface_ctx * if_ctx)
{
    DEBUGMSGTL(("internal:pstatsTable:_pstatsTable_container_init",
                "called\n"));

    /*
     * cache init
     */
    if_ctx->cache = netsnmp_cache_create(30,    /* timeout in seconds */
                                         _cache_load, _cache_free,
                                         pstatsTable_oid,
                                         pstatsTable_oid_size);

    if (NULL == if_ctx->cache) {
        snmp_log(LOG_ERR, "error creating cache for pstatsTable\n");
        return;
    }

    if_ctx->cache->flags = NETSNMP_CACHE_DONT_INVALIDATE_ON_SET;

    pstatsTable_container_init(&if_ctx->container, if_ctx->cache);
    if (NULL == if_ctx->container)
        if_ctx->container =
            netsnmp_container_find("pstatsTable:table_container");
    if (NULL == if_ctx->container) {
        snmp_log(LOG_ERR, "error creating container in "
                 "pstatsTable_container_init\n");
        return;
    }

    if (NULL != if_ctx->cache)
        if_ctx->cache->magic = (void *) if_ctx->container;
}                               /* _pstatsTable_container_init */

/**
 * @internal
 * shutdown the container with functions or wrappers
 */
void
_pstatsTable_container_shutdown(pstatsTable_interface_ctx * if_ctx)
{
    DEBUGMSGTL(("internal:pstatsTable:_pstatsTable_container_shutdown",
                "called\n"));

    pstatsTable_container_shutdown(if_ctx->container);

    _container_free(if_ctx->container);

}                               /* _pstatsTable_container_shutdown */


pstatsTable_rowreq_ctx *
pstatsTable_row_find_by_mib_index(pstatsTable_mib_index * mib_idx)
{
    pstatsTable_rowreq_ctx *rowreq_ctx;
    oid             oid_tmp[MAX_OID_LEN];
    netsnmp_index   oid_idx;
    int             rc;

    /*
     * set up storage for OID
     */
    oid_idx.oids = oid_tmp;
    oid_idx.len = sizeof(oid_tmp) / sizeof(oid);

    /*
     * convert
     */
    rc = pstatsTable_index_to_oid(&oid_idx, mib_idx);
    if (MFD_SUCCESS != rc)
        return NULL;

    rowreq_ctx =
        (pstatsTable_rowreq_ctx *) CONTAINER_FIND(pstatsTable_if_ctx.
                                                  container, &oid_idx);

    return rowreq_ctx;
}
