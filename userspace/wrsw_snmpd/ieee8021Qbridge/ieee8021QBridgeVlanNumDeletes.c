/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.scalar.conf 17337 2009-01-01 14:28:29Z magfr $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "ieee8021QBridgeVlanNumDeletes.h"

/** Initializes the ieee8021QBridgeVlanNumDeletes module */
void
init_ieee8021QBridgeVlanNumDeletes(void)
{
    const oid ieee8021QBridgeVlanNumDeletes_oid[] = { 1,3,111,2,802,1,1,4,1,4,1 };

  DEBUGMSGTL(("ieee8021QBridgeVlanNumDeletes", "Initializing\n"));

    netsnmp_register_scalar(
        netsnmp_create_handler_registration("ieee8021QBridgeVlanNumDeletes", handle_ieee8021QBridgeVlanNumDeletes,
                               ieee8021QBridgeVlanNumDeletes_oid, OID_LENGTH(ieee8021QBridgeVlanNumDeletes_oid),
                               HANDLER_CAN_RONLY
        ));
}

int
handle_ieee8021QBridgeVlanNumDeletes(netsnmp_mib_handler *handler,
                          netsnmp_handler_registration *reginfo,
                          netsnmp_agent_request_info   *reqinfo,
                          netsnmp_request_info         *requests)
{
    /* We are never called for a GETNEXT if it's registered as a
       "instance", as it's "magically" handled for us.  */

    /* a instance handler also only hands us one request at a time, so
       we don't need to loop over a list of requests; we'll only get one. */
    
    switch(reqinfo->mode) {

        case MODE_GET:
            snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
                                     (u_char *) /* XXX: a pointer to the scalar's data */,
                                     /* XXX: the length of the data in bytes */);
            break;


        default:
            /* we should never get here, so this is a really bad error */
            snmp_log(LOG_ERR, "unknown mode (%d) in handle_ieee8021QBridgeVlanNumDeletes\n", reqinfo->mode );
            return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
